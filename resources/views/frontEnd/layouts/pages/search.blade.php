@extends('frontEnd.layouts.master')
@section('title','Search: '.$keyword)
@section('breadcrumb')Search results for “{{ $keyword }}”@endsection
@push('css')<style>.search-page{padding:32px 0}.search-page h1{font-size:22px;font-weight:800}.search-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px}.search-card{border:1px solid #e5e5e5;padding:8px}.search-card img{width:100%;aspect-ratio:1;object-fit:cover}.search-card h5{font-size:12px;font-weight:800;min-height:35px;margin:8px 0}.search-card strong{font-size:14px}@media(max-width:767px){.search-grid{grid-template-columns:repeat(2,1fr);gap:9px}.search-page{padding:20px 0}}</style>@endpush
@section('content')<div class="fashion-container search-page"><h1>Search results for “{{ $keyword }}”</h1><p class="text-muted small">{{ $products->total() }} product found</p><div class="search-grid">@forelse($products as $product)<article class="search-card"><a href="{{ route('product',$product->slug) }}"><img src="{{ asset(optional($product->image)->image ?: 'public/uploads/default/product.png') }}" alt="{{ $product->name }}"><h5>{{ Str::limit($product->name,55) }}</h5><strong>৳{{ number_format($product->new_price) }}</strong>@if($product->old_price)<del class="small text-muted ms-1">৳{{ number_format($product->old_price) }}</del>@endif</a></article>@empty<p>No product found. Try another keyword.</p>@endforelse</div><div class="mt-4">{{ $products->withQueryString()->links('pagination::bootstrap-4') }}</div></div>@endsection
