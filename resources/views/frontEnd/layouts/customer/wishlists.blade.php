@extends('frontEnd.layouts.master')
@section('title', 'My Wishlist')
@section('content')
<section class="customer-section"><div class="container"><div class="row"><div class="col-sm-3"><div class="customer-sidebar">@include('frontEnd.layouts.customer.sidebar')</div></div><div class="col-sm-9"><div class="customer-content"><h5 class="account-title">My Wishlist</h5>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="row g-3">@forelse($wishlists as $wishlist) @php($product = $wishlist->product) @if($product)<div class="col-6 col-md-4"><div class="card h-100 border"><a href="{{ route('product', $product->slug) }}"><img src="{{ asset(optional($product->image)->image ?: 'public/uploads/default/product.png') }}" class="card-img-top" style="aspect-ratio:1;object-fit:cover" alt="{{ $product->name }}"></a><div class="card-body p-3"><a class="text-dark fw-bold text-decoration-none" href="{{ route('product', $product->slug) }}">{{ Str::limit($product->name, 48) }}</a><p class="text-success fw-bold my-2">৳{{ number_format($product->new_price) }}</p><div class="d-flex gap-1"><a href="{{ route('product', $product->slug) }}" class="btn btn-sm btn-success flex-grow-1">দেখুন</a><form method="POST" action="{{ route('customer.wishlist.destroy', $wishlist) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" title="Remove" onclick="return confirm('Wishlist থেকে সরাবেন?')"><i class="fa fa-trash"></i></button></form></div></div></div></div>@endif @empty<div class="col-12 text-center py-5"><i class="fa fa-heart fs-1 text-muted"></i><h5 class="mt-3">আপনার wishlist এখনো খালি</h5><p class="text-muted">পছন্দের পণ্য wishlist-এ রাখুন, পরে সহজে খুঁজে পাবেন।</p><a class="btn btn-success" href="{{ route('home') }}">কেনাকাটা শুরু করুন</a></div>@endforelse</div><div class="mt-4">{{ $wishlists->links() }}</div></div></div></div></div></section>
@endsection
