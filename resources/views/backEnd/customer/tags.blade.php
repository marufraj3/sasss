@extends('backEnd.layouts.master')
@section('title', 'Customer CRM Tags')
@section('content')
<div class="container-fluid"><div class="page-title-box"><h4 class="page-title mb-1">Customer CRM tags</h4><p class="text-muted mb-0">Tag customers as VIP, repeat buyer, wholesale, high-risk, influencer and more.</p></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="row"><div class="col-lg-4"><div class="card"><div class="card-body"><h5>Create tag</h5><form method="POST" action="{{ route('customer-tags.store') }}">@csrf<div class="mb-3"><label>Name</label><input class="form-control @error('name') is-invalid @enderror" name="name" required placeholder="VIP Customer">@error('name')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="mb-3"><label>Color</label><input class="form-control form-control-color" type="color" name="color" value="#087f5b"></div><button class="btn btn-primary">Create tag</button></form></div></div></div><div class="col-lg-8"><div class="card"><div class="card-body"><table class="table"><thead><tr><th>Tag</th><th>Customers</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($tags as $tag)<tr><td><span class="badge" style="background:{{ $tag->color }}">{{ $tag->name }}</span></td><td>{{ $tag->customers_count }}</td><td class="text-end"><form method="POST" action="{{ route('customer-tags.destroy', $tag) }}" class="d-inline" onsubmit="return confirm('Delete this tag?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="3" class="text-center text-muted">No tags yet.</td></tr>@endforelse</tbody></table></div></div></div></div></div>
@endsection
