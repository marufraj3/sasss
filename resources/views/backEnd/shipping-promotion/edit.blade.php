@extends('backEnd.layouts.master')
@section('title', 'Free Delivery Promotion')
@section('content')
<div class="container-fluid"><div class="page-title-box"><h4 class="page-title mb-1">Free delivery promotion</h4><p class="text-muted mb-0">Increase average cart value by offering free delivery above a minimum order amount.</p></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="card"><div class="card-body"><form method="POST" action="{{ route('shipping-promotion.update') }}" class="row g-3">@csrf<div class="col-md-6"><label class="form-label">Minimum order for free delivery (৳)</label><input type="number" min="1" name="minimum_order" class="form-control" value="{{ old('minimum_order', $promotion->minimum_order) }}" required></div><div class="col-md-6"><label class="form-label">Customer message</label><input name="message" class="form-control" value="{{ old('message', $promotion->message) }}" placeholder="আর ৳X কিনলেই ফ্রি ডেলিভারি"></div><div class="col-12"><div class="form-check form-switch"><input class="form-check-input" type="checkbox" name="status" value="1" id="status" @checked(old('status', $promotion->status))><label class="form-check-label" for="status">Enable this promotion</label></div></div><div class="col-12"><button class="btn btn-primary">Save promotion</button></div></form></div></div></div>
@endsection
