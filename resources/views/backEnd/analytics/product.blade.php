@extends('backEnd.layouts.master')
@section('title', 'Product Conversion Analytics')
@section('content')
<div class="container-fluid"><div class="page-title-box d-flex justify-content-between align-items-center"><div><h4 class="page-title mb-1">Product conversion analytics</h4><p class="text-muted mb-0">View → Add to cart → Completed order funnel</p></div><form><select name="days" class="form-select" onchange="this.form.submit()"><option value="7" @selected($days === 7)>Last 7 days</option><option value="30" @selected($days === 30)>Last 30 days</option><option value="90" @selected($days === 90)>Last 90 days</option><option value="365" @selected($days === 365)>Last year</option></select></form></div>
<div class="row g-3 mb-4"><div class="col-md-4"><div class="card"><div class="card-body"><small class="text-muted">Product page views</small><h3>{{ number_format($summary['views']) }}</h3></div></div></div><div class="col-md-4"><div class="card"><div class="card-body"><small class="text-muted">Add to cart events</small><h3 class="text-primary">{{ number_format($summary['cart_adds']) }}</h3></div></div></div><div class="col-md-4"><div class="card"><div class="card-body"><small class="text-muted">Completed units ordered</small><h3 class="text-success">{{ number_format($summary['completed_units']) }}</h3></div></div></div></div>
<div class="card"><div class="card-body table-responsive"><table class="table table-hover align-middle"><thead><tr><th>Product</th><th class="text-center">Views</th><th class="text-center">Cart adds</th><th class="text-center">Cart rate</th><th class="text-center">Completed units</th><th class="text-end">Completed revenue</th></tr></thead><tbody>@forelse($products as $product)@php($cartRate = $product->views ? round(($product->cart_adds / $product->views) * 100, 1) : 0)<tr><td><a href="{{ route('product', $product->slug) }}" target="_blank" class="fw-semibold text-dark">{{ $product->name }}</a></td><td class="text-center">{{ number_format($product->views) }}</td><td class="text-center">{{ number_format($product->cart_adds) }}</td><td class="text-center"><span class="badge bg-soft-primary text-primary">{{ $cartRate }}%</span></td><td class="text-center">{{ number_format($product->units_ordered) }}</td><td class="text-end">৳{{ number_format($product->revenue) }}</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">এখনো event data পাওয়া যায়নি। Product page view ও cart add হওয়ার পর data আসবে।</td></tr>@endforelse</tbody></table><div>{{ $products->links() }}</div></div></div></div>
@endsection
