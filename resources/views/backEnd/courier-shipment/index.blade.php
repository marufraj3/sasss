@extends('backEnd.layouts.master')
@section('title', 'Courier Shipments')
@section('content')
<div class="container-fluid"><div class="page-title-box"><h4 class="page-title mb-1">Courier dispatch & tracking</h4><p class="text-muted mb-0">Consignment/tracking number save করুন এবং delivery status manage করুন।</p></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="card mb-4"><div class="card-body"><h5 class="mb-3">Create or update shipment</h5><form method="POST" action="{{ route('courier-shipments.store') }}" class="row g-3">@csrf<div class="col-md-4"><label class="form-label">Order</label><select class="form-select" name="order_id" required><option value="">Select order</option>@foreach($orders as $order)<option value="{{ $order->id }}">#{{ $order->invoice_id }} — {{ optional($order->shipping)->name }} @if($order->courierShipment) ({{ $order->courierShipment->tracking_number }}) @endif</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">Courier</label><select class="form-select" name="courier_name" required>@foreach($couriers as $courier)<option value="{{ $courier->type }}">{{ ucfirst($courier->type) }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">Tracking / Consignment ID</label><input class="form-control" name="tracking_number" required></div><div class="col-md-3"><label class="form-label">Status</label><select class="form-select" name="shipment_status"><option value="booked">Booked</option><option value="picked_up">Picked up</option><option value="in_transit">In transit</option><option value="delivered">Delivered</option><option value="failed_delivery">Failed delivery</option><option value="returned">Returned</option></select></div><div class="col-12"><input class="form-control" name="note" placeholder="Internal shipment note (optional)"></div><div class="col-12"><button class="btn btn-primary">Save shipment</button></div></form></div></div>
<div class="card"><div class="card-body table-responsive"><table class="table table-hover align-middle"><thead><tr><th>Order</th><th>Customer</th><th>Courier / Tracking</th><th>Dispatched</th><th>Status</th></tr></thead><tbody>@forelse($shipments as $shipment)<tr><td>#{{ optional($shipment->order)->invoice_id }}</td><td>{{ optional(optional($shipment->order)->shipping)->name }}<br><small>{{ optional(optional($shipment->order)->shipping)->phone }}</small></td><td><strong>{{ ucfirst($shipment->courier_name) }}</strong><br><code>{{ $shipment->tracking_number }}</code></td><td>{{ optional($shipment->dispatched_at)->format('d M Y, h:i A') ?: 'Not dispatched' }}</td><td><form method="POST" action="{{ route('courier-shipments.status', $shipment) }}" class="d-flex gap-2">@csrf<select name="shipment_status" class="form-select form-select-sm">@foreach(['booked'=>'Booked','picked_up'=>'Picked up','in_transit'=>'In transit','delivered'=>'Delivered','failed_delivery'=>'Failed delivery','returned'=>'Returned'] as $key=>$label)<option value="{{ $key }}" @selected($shipment->shipment_status === $key)>{{ $label }}</option>@endforeach</select><button class="btn btn-sm btn-outline-success">Save</button></form></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No courier shipments yet.</td></tr>@endforelse</tbody></table><div>{{ $shipments->links() }}</div></div></div></div>
@endsection
