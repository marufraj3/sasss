@extends('backEnd.layouts.master')
@section('title', 'Return Requests')
@section('content')
<div class="container-fluid"><div class="page-title-box"><h4 class="page-title mb-1">Return & refund requests</h4><p class="text-muted mb-0">Approve করলে order-এর product stock automatically restore হবে।</p></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="btn-group mb-3">@foreach(['pending'=>'Pending','approved'=>'Approved','rejected'=>'Rejected'] as $key=>$label)<a class="btn btn-sm {{ $status === $key ? 'btn-primary' : 'btn-outline-primary' }}" href="{{ route('return-requests.index', ['status'=>$key]) }}">{{ $label }} ({{ $counts[$key] }})</a>@endforeach</div>
<div class="card"><div class="card-body table-responsive"><table class="table table-hover align-middle"><thead><tr><th>Order / Customer</th><th>Reason</th><th>Products</th><th>Requested</th><th class="text-end">Action</th></tr></thead><tbody>@forelse($requests as $return)<tr><td><strong>#{{ optional($return->order)->invoice_id }}</strong><br><small>{{ optional($return->customer)->name }} · {{ optional($return->customer)->phone }}</small></td><td>{{ $return->reason }}@if($return->admin_note)<br><small class="text-muted">Admin: {{ $return->admin_note }}</small>@endif</td><td><small>@foreach(optional($return->order)->orderdetails ?? [] as $detail){{ $detail->product_name }} × {{ $detail->qty }}@if(!$loop->last)<br>@endif @endforeach</small></td><td>{{ $return->created_at->format('d M, h:i A') }}</td><td class="text-end">@if($return->status === 'pending')<button class="btn btn-sm btn-success" data-bs-toggle="modal" data-bs-target="#approve{{ $return->id }}">Approve</button><button class="btn btn-sm btn-outline-danger" data-bs-toggle="modal" data-bs-target="#reject{{ $return->id }}">Reject</button><div class="modal fade" id="approve{{ $return->id }}" tabindex="-1"><div class="modal-dialog"><form method="POST" action="{{ route('return-requests.approve', $return) }}" class="modal-content">@csrf<div class="modal-header"><h5 class="modal-title">Approve return</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><p>Product stock পুনরায় যোগ হবে। নিশ্চিত করুন পণ্যটি ফেরত পেয়েছেন।</p><textarea class="form-control" name="admin_note" placeholder="Optional note"></textarea></div><div class="modal-footer"><button class="btn btn-success">Approve & restore stock</button></div></form></div></div><div class="modal fade" id="reject{{ $return->id }}" tabindex="-1"><div class="modal-dialog"><form method="POST" action="{{ route('return-requests.reject', $return) }}" class="modal-content">@csrf<div class="modal-header"><h5 class="modal-title">Reject return</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><textarea class="form-control" name="admin_note" required placeholder="Customer-কে জানানোর কারণ লিখুন"></textarea></div><div class="modal-footer"><button class="btn btn-danger">Reject request</button></div></form></div></div>@else<span class="badge {{ $return->status === 'approved' ? 'bg-success' : 'bg-danger' }}">{{ ucfirst($return->status) }}</span>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">কোনো return request নেই।</td></tr>@endforelse</tbody></table><div>{{ $requests->links() }}</div></div></div></div>
@endsection
